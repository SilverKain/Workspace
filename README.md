# Markdown Viewer - Локальное веб-приложение

## 🎨 ДИЗАЙН В СТИЛЕ NOTEBOOKLM

Современный, минималистичный интерфейс вдохновленный Google NotebookLM с акцентом на:
- Чистое белое пространство
- Мягкие тени и закругленные углы
- Карточный дизайн
- Интуитивная типографика
- Плавные анимации

## 📋 ИНСТРУКЦИЯ ПО ЗАПУСКУ

1. **Открыть приложение:**
   - Просто откройте файл `index.html` в любом современном браузере
   - Двойной клик на `index.html` ИЛИ
   - Правой кнопкой мыши → Открыть с помощью → Браузер
   - Приложение работает полностью локально (file://)

2. **Загрузка файлов:**
   - Нажмите кнопку "Загрузить файлы" в нижней части левой панели
   - Выберите один или несколько .md или .txt файлов
   - Файлы будут автоматически загружены и сохранены в браузере
   - Первый файл откроется автоматически

3. **Работа с проектами:**
   - Нажмите "Создать проект" в верхней части левой панели
   - Введите название проекта
   - Перетащите файлы из секции "Источники" в проект
   - Кликните на файл в проекте для просмотра
   - Создавайте структуру и связи между файлами
   - Разворачивайте/сворачивайте проекты кликом

4. **Перетаскивание файлов:**
   - Возьмите файл из списка "Источники" (нижняя часть)
   - Перетащите его в зону проекта (верхняя часть)
   - Файл будет добавлен в проект
   - Можно добавлять один файл в несколько проектов

5. **Просмотр URL:**
   - Введите адрес сайта в поле "Введите URL..."
   - Нажмите "Открыть URL"
   - Сайт откроется в iframe в центральной панели
   - Нажмите "Вернуться к файлу" для возврата

5. **Календарь и статистика:**
   - Правая панель показывает текущий месяц
   - Дни с активностью выделены жёлтым цветом
   - Кликните на день для просмотра статистики
   - Видно количество открытий каждого файла за день

## 🗑️ КАК ОЧИСТИТЬ ДАННЫЕ

### Способ 1: Кнопкой в интерфейсе (рекомендуется)
1. В правой панели под календарём нажмите кнопку **"Очистить память"**
2. Подтвердите удаление
3. Страница перезагрузится, данные localStorage будут очищены

### Способ 2: Через консоль браузера
1. Откройте консоль (F12 → Console)
2. Введите: `clearAllData()`
3. Подтвердите удаление

### Способ 3: Вручную
1. Откройте DevTools (F12)
2. Перейдите на вкладку Application / Хранилище
3. Найдите Local Storage → file://
4. Удалите все ключи с префиксом `markdownApp_`

### Способ 4: Очистка всего localStorage
1. DevTools (F12) → Application → Local Storage
2. Правой кнопкой на file:// → Clear

## 💾 ГДЕ ХРАНЯТСЯ ДАННЫЕ

Все данные хранятся в **localStorage** браузера:

- `markdownApp_files` - загруженные файлы (название + содержимое)
- `markdownApp_currentFile` - последний открытый файл
- `markdownApp_statistics` - статистика открытий по датам
- `markdownApp_projects` - список проектов с файлами
- `markdownApp_projectIdCounter` - счётчик ID проектов

**Важно:**
- Данные привязаны к браузеру
- Каждый браузер имеет своё хранилище
- Очистка данных браузера удалит файлы и проекты
- Данные НЕ синхронизируются между устройствами
- Проекты хранят только ссылки на файлы, не дублируют содержимое

### Основные функции (app.js):

#### Управление состоянием:
- `AppState` - глобальный объект состояния (файлы, проекты, статистика)
- `saveToLocalStorage()` - сохранение данных
- `loadFromLocalStorage()` - загрузка при старте
- `clearAllData()` - полная очистка localStorage (кнопка "Очистить память" и консоль)

#### Работа с файлами:
- `handleFileSelect()` - FileReader API для чтения файлов
- `displayFile()` - рендер Markdown в HTML
- `recordFileOpen()` - запись статистики
- `renderFileList()` - отображение списка файлов

#### Работа с проектами:
- `handleAddProject()` - создание нового проекта
- `renderProjectList()` - отображение списка проектов
- `createProjectElement()` - создание DOM элемента проекта
- `toggleProject()` - развернуть/свернуть проект
- `renameProject()` - переименование проекта
- `deleteProject()` - удаление проекта
- `removeFileFromProject()` - удаление файла из проекта

#### Drag and Drop:
- `handleFileDragStart()` - начало перетаскивания файла
- `handleFileDragEnd()` - окончание перетаскивания
- `handleProjectDragOver()` - файл над зоной проекта
- `handleProjectDragLeave()` - файл покинул зону
- `handleProjectDrop()` - файл сброшен в проект

#### Календарь:
- `renderCalendar()` - отрисовка месяца
- `renderCalendarDays()` - генерация дней
- Навигация по месяцам (← →)
- Клик на день → статистика

#### Статистика:
- `renderStats()` - отображение статистики
- `renderDateStats()` - статистика за день
- `renderOverallStats()` - общая статистика

#### URL/iframe:
- `handleUrlLoad()` - загрузка URL в iframe
- `handleBackToFile()` - возврат к файлу
- Автодобавление https://

### Особенности реализации:

1. **Дизайн в стиле NotebookLM**
   - Современные цвета и тени
   - Карточный дизайн элементов
   - CSS переменные для темы
   - Google Sans шрифт (с fallback)
   - Плавные анимации и переходы

2. **Двухсекционная левая панель**
   - Flex-контейнер с двумя секциями
   - Верхняя секция: Проекты
   - Нижняя секция: Источники
   - Независимая прокрутка секций
   - Четкое разделение проектов и источников

3. **Система проектов**
   - Группировка файлов по проектам
   - Множественные проекты
   - Один файл в нескольких проектах
   - Визуальная структура файлов
   - **Клик на файл в проекте для просмотра**
   - Подсветка активного файла в проектах

4. **Native Drag and Drop API**
   - HTML5 dragabble атрибут
   - DataTransfer для передачи данных
   - Визуальная обратная связь
   - Валидация перед добавлением

5. **Полностью статичное приложение**
   - Работает без сервера
   - Открывается через file://
   - Все данные в браузере

6. **Без сборщиков**
   - Нет webpack, vite, parcel
   - Нет транспиляции
   - Прямое подключение скриптов

7. **Без fetch**
   - FileReader API для чтения файлов
   - CDN для marked.js (подключается через <script>)
   - localStorage для хранения

8. **Календарь на чистом JS**
   - Без moment.js, date-fns
   - Нативный объект Date
   - Собственная логика рендеринга

### Технологии:

#### Используются:
- ✅ HTML5
- ✅ CSS3 (Grid, Flexbox, Variables)
- ✅ Vanilla JavaScript (ES6+)
- ✅ marked.js (CDN) - парсинг Markdown
- ✅ localStorage API
- ✅ FileReader API
- ✅ Drag and Drop API
- ✅ SVG иконки

#### НЕ используются:
- ❌ Node.js
- ❌ npm / yarn
- ❌ webpack / vite
- ❌ React / Vue / Angular
- ❌ Backend / сервер
- ❌ fetch / AJAX
- ❌ jQuery
- ❌ Bootstrap / Tailwind
- ❌ Moment.js / date-fns

## ⚠️ ОГРАНИЧЕНИЯ

- Размер localStorage ограничен (~5-10 MB)
- Большие файлы могут не поместиться
- iframe может блокировать некоторые сайты (CORS, X-Frame-Options)
- Данные хранятся только в одном браузере
- Нет облачной синхронизации

## 🔧 РАСШИРЕНИЕ ФУНКЦИОНАЛА

Если хотите добавить новые функции, можно:
1. Связи между файлами в проектах (граф)
2. Визуализация структуры проекта
3. Экспорт проектов в JSON
4. Импорт проектов из JSON
5. Поиск по содержимому файлов
6. Тёмная тема
7. Markdown редактор
8. Экспорт статистики в CSV
9. Теги для файлов
10. Заметки к проектам

## 📝 ЛИЦЕНЗИЯ

Свободное использование. Никаких ограничений.

## 💡 СОВЕТЫ

- Создавайте проекты для группировки связанных файлов
- Используйте drag-and-drop для быстрого добавления файлов
- Один файл может быть в нескольких проектах одновременно
- Делайте резервные копии важных файлов
- Не загружайте слишком большие файлы (>1 MB)
- Периодически очищайте старые данные
- Используйте современные браузеры (Chrome, Firefox, Edge)

---

**Приложение готово к использованию!** 🚀
Просто откройте `index.html` и начните работать с современным интерфейсом в стиле NotebookLM!

## 🎯 ОСНОВНЫЕ ВОЗМОЖНОСТИ

✅ Загрузка нескольких Markdown-файлов одновременно
✅ Хранение файлов в браузере (не теряются после перезагрузки)
✅ Рендеринг Markdown в красивый HTML
✅ **Создание проектов для организации файлов**
✅ **Drag-and-drop файлов в проекты**
✅ **Просмотр файлов прямо из проектов**
✅ **Управление структурой проектов**
✅ Подсчёт открытий каждого файла
✅ Календарь с визуализацией активности
✅ Статистика по дням
✅ Просмотр веб-страниц через iframe
✅ Сохранение последнего открытого файла
✅ **Современный дизайн в стиле NotebookLM**
✅ Полностью локальная работа

## 🏗️ АРХИТЕКТУРА ПРИЛОЖЕНИЯ

### Структура файлов:
```
/
├── index.html    - HTML-разметка приложения
├── style.css     - Стили в стиле NotebookLM
├── app.js        - Вся логика приложения
└── README.md     - Эта инструкция
```

### Основные компоненты:

#### 1. **Layout (CSS Grid)**
- Трёхколоночная структура
- Левая панель: 280px (разделена на 2 секции)
  - Верхняя: Проекты
  - Нижняя: Источники (загруженные файлы)
- Центральная панель: flex (1fr)
- Правая панель: 300px
- Высота: 100vh (на весь экран)

#### 2. **Левая панель - Проекты (верхняя секция)**
- Кнопка "Создать проект"
- Список проектов с файлами
- Развернуть/свернуть проекты
- Переименование проектов
- Удаление проектов
- **Клик на файл в проекте для просмотра**
- **Drop zone для принятия файлов**
- Удаление файлов из проектов

#### 3. **Левая панель - Источники (нижняя секция)**
- Кнопка "Загрузить файлы"
- Скрытый `<input type="file" multiple>`
- Список загруженных файлов
- Подсветка активного файла
- Счётчики открытий
- **Поддержка drag для перетаскивания в проекты**

#### 4. **Центральная панель (Контент)**
- Поле для ввода URL
- Кнопки управления (Открыть/Вернуться)
- Область контента:
  - Приветственный экран (по умолчанию)
  - Отрендеренный Markdown
  - iframe для внешних URL
- Использует **marked.js** (CDN) для Markdown

#### 5. **Правая панель (Календарь)**
- Самописный JS-календарь
- Навигация по месяцам
- Подсветка:
  - Сегодняшний день (голубой)
  - Дни с активностью (жёлтый)
  - Выбранный день (синий)
- Счётчики открытий на каждый день
- Секция статистики:
  - Общая статистика
  - Статистика за выбранный день